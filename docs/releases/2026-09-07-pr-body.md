Bring the current runtime and public interface into selfmd/main. The existing main lacks later security and delivery fixes; this integration adds authenticated reliable delivery, persisted receipts and retries, group-manifest synchronization, and the public/operator interface with explicit publication controls.

Preserves the newer design and security implementation when reconciling the historical dashboard and policy branches. Policy PRs #2–#6 remain a separate forward-port stage; this PR does not enable agent tool execution.

Fixes a deployment blocker discovered during verification: the Docker image built successfully but failed at startup because production pruning removed workspace dependencies. Uses a frozen production install and adds a container startup/authentication smoke check to CI. Both CI jobs initialize the pinned docs submodule; package verification includes its exact commit.

Compatibility: handshake v3 requires coordinated peer upgrades; database migration reaches schema 9. MCP identity public keys use hex. Back up data and secrets before migration; rollback restores the prior data snapshot with the prior application version.

Validation: frozen pnpm 10.33.2 install, full production build including docs, workspace and dashboard-client TypeScript, 503 tests, five clean-archive npm tarball consumer checks, Linux arm64/Node 22 Docker build and live container publication/authentication checks passed. Production rollout and amd64 execution have not been verified.

See docs/releases/2026-09-07-integration.md for source reconciliation and rollout steps.
